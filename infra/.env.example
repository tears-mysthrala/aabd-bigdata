# Infra compartida (frontal + bus). SOLO-LAB: frontal y Kafka solo en loopback.
# Uso: `cp .env.example .env` (opcional; hay defaults) y `docker compose up -d` ANTES que los labs.
KAFKA_PORT=9092
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
