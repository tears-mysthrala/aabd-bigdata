# Solo actions (auditoría semanal ligera). Sin CI en pushes: ver workflows/validate.yml.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 3
